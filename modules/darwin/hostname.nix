{
  networking = {
    hostName = "beast";        # scutil --set HostName       (CLI / SSH)
    localHostName = "beast";   # scutil --set LocalHostName  (Bonjour / .local)
    computerName = "beast";    # scutil --set ComputerName   (Finder ▸ Sharing)
  };
}
